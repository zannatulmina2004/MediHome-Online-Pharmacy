# MediHome configuration
SECRET_KEY=replace-with-a-long-random-secret

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=medihome2

# Optional: configure Gmail SMTP for verification/order emails.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=MediHome <noreply@example.com>
